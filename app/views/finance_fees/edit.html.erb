<div class="content-box">
  <div class="content-box-header">
    <h3>更新报关号为<%= @finance.declaration.entry_no %>的账单的付费信息</h3>
    <div class="operations">
      <%= link_to '返回账单', finance_path(@finance) %>
    </div>
  </div>
  <div class="content-box-content">
    <%= render 'form' %>
  </div>
</div>

<% id =  attribute.to_s + "_autocomplete" %>
<% label_id = id + "_label" %>
<%= form.text_field attribute, :class => klass, :id => id, :"data-url" => url %>
<br />
<small id="<%= label_id %>"></small>
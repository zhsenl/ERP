<%= render "shared/enterprise_selector" %>

<div class="content-box">
	<div class="content-box-header">
		<h3><%= @declaration_type == "export" ? "出口" : "进口"  %>报关单列表</h3>
		<div class="operations">
			<%= link_to "添加新#{@declaration_type == 'export' ? '出口' : '进口'}报关单", new_declaration_path(:declaration_type => @declaration_type) %>
		</div>
	</div>
	<div class="content-box-content">
		<div class="tab-content default-tab">
			<table>
				<thead>
					<tr>
						<th>预录入号</th>
						<th>申报日期</th>
						<th>操作</th>
					</tr>
				</thead>
				<tfoot>
					<tr>
						<td colspan="6"> <%= will_paginate @declarations %> </td>
					</tr>
				</tfoot>
				<tbody>
					<% @declarations.each do |declaration| %>
					<tr>
						<td><%= link_to declaration.pre_entry_no, declaration %></td>
						<td><%= declaration.declare_date.strftime('%Y-%m-%d') %></td>
						<td> <%= link_to image_tag("icons/pencil.png", :alt => "Edit"), edit_declaration_path(declaration) %>
						<%= link_to image_tag("icons/cross.png", :alt => "Delete"), declaration, confirm: '确定删除?', method: :delete %> </td>
					</tr>
					<% end %>
				</tbody>
			</table>
		</div>
	</div>
</div>

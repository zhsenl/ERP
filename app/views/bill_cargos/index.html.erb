<h1>Listing bill_cargos</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bill_cargos.each do |bill_cargo| %>
  <tr>
    <td><%= link_to 'Show', bill_cargo %></td>
    <td><%= link_to 'Edit', edit_bill_cargo_path(bill_cargo) %></td>
    <td><%= link_to 'Destroy', bill_cargo, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bill cargo', new_bill_cargo_path %>

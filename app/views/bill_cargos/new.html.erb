<div class="content-box">
  <div class="content-box-header">
    <h3>添加新<%= @sys_type == "return" ? "退货" : "收发货" %>(<%= params[:in_out] == '0' ? '转出':'转入' %>)</h3>
    <div class="operations">
      <%= link_to '返回列表', bill_cargos_path(:bill_id => @bill.id) %>
    </div>
  </div>
  <div class="content-box-content">
    <%= render 'form' %>
  </div>
</div>
